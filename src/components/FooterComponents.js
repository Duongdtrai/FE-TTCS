import React from 'react'

const FooterComponents = () => {
    return (
        <div>
            <footer className = "footer">
                 <span className="text-muted">Dung dep trai</span>
            </footer>
        </div>
    )
}

export default FooterComponents